import { FirstHighlightDirective } from './first-highlight.directive';

describe('FirstHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
